import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.segihovav.sliderclock',
  appName: 'SliderClock',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
